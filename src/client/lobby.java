package src.client;

import javax.swing.JFrame;

public class lobby extends JFrame {
    public lobby() {
        setTitle("lobby placeholder");
        setSize(400, 300);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setVisible(true);
    }
}